// The page body lives in components/StockMonitorDashboard.tsx, shared with
// the top-level /stock-monitoring route (which exists so it can be pinned
// to a home-screen icon with its own manifest/start_url - see
// app/stock-monitoring/layout.tsx).
export { default } from '@/components/StockMonitorDashboard'
